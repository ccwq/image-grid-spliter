## 1. 导出基础模块

- [x] 1.1 新建 `useDirectoryExport` module，封装功能检测、IndexedDB 目录句柄持久化、权限查询/请求和更换目录。
- [x] 1.2 在 module 内实现 `TileResult` Blob 的直接写入、`-1` 递增重名规则和结构化的完整/部分失败结果。
- [x] 1.3 保留 `useImageSlicer` 的传统逐张下载 helper，作为唯一降级实现。

## 2. 主流程与界面

- [x] 2.1 调整“下载全部”时序，确保首次目录选择/重新授权发生在用户点击路径，并把成功、取消、拒绝和无支持情况映射到智能导出或传统下载。
- [x] 2.2 自动下载仅在已有可写目录时走直写；否则保持既有传统逐张下载。
- [x] 2.3 在 `ExportSettings` 增加受功能检测保护的“更换导出目录”，在不支持的浏览器显示传统下载说明并隐藏控制。
- [x] 2.4 增加本地化状态/错误文案，并提供部分失败结果与“以传统下载重试未完成项”的显式操作。

## 3. 验证

- [x] 3.1 为目录 module 编写 Vitest 单元测试。每个新增/修改测试用例前添加中文 Given/When/Then/防回归注释，覆盖支持检测、权限状态、句柄持久化、`-1` 重名与部分失败结果。
- [ ] 3.2 为 `useImageSlicer` 或导出交互编写测试，覆盖传统回退、自动下载无首次 picker、用户明确重试未完成项。
- [ ] 3.3 在 Chrome/Edge 手工验证首次选目录、刷新后复用/重新授权、更换目录、重名和部分失败；在不支持浏览器验证传统逐张下载提示及入口。
- [x] 3.4 执行 `pnpm test`、`pnpm exec vue-tsc --noEmit`、`pnpm build` 和 `openspec validate add-directory-batch-export --strict`。
