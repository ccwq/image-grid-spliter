## 0. 预研与选型
- [x] 0.1 调研第三方预览库（触摸滑动、缩放、下载按钮可扩展），评估体积、许可与 Vue 集成成本。
- [x] 0.2 确定优先选型（如 PhotoSwipe MIT）并给出集成方案与降级备选。

## 1. 需求与设计
- [x] 1.1 明确预览入口、退出与切片顺序（与现有结果列表/下载行为保持一致）。
- [x] 1.2 确定左右滑动/按钮/键盘的切换策略与移动端手势范围，定义缩放边界与默认缩放重置策略。
- [x] 1.3 定义预览态下载的文件命名与用户提示，确保与批量下载命名一致。

## 2. 实现
- [x] 2.1 建立预览态状态管理（当前索引、缩放、动画），接入现有切片存储并支持新增切片时自动更新列表。
- [x] 2.2 实现左右滑动/按钮/键盘切换交互，处理切片不存在、边界回弹或循环策略（按设计落地）。
- [x] 2.3 实现缩放与拖动（捏合/滚轮/按钮），加入最小/最大缩放限制与重置控制，兼容移动端手势。
- [x] 2.4 在预览态提供单张下载动作，复用现有下载逻辑/命名，确保不会阻断后续导航。
- [x] 2.5 添加 UI 提示（页码/索引、缩放提示），并确保动画/触摸反馈流畅。

## 3. 测试与验证
- [ ] 3.1 单元/组件测试：预览状态更新、新切片追加后的可见性、缩放边界、下载触发回调。
- [ ] 3.2 手动验证：移动端左右滑动/捏合缩放、桌面滚轮缩放与键盘切换、预览态下载文件命名、切片新增时的列表更新。

## 4. 文档
- [x] 4.1 更新 README/指南，描述预览入口、滑动缩放操作与下载提示。
