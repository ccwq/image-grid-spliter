## 1. 现象复现与定位
- [ ] 1.1 在移动端或模拟器中复现拖拽上传重复入队，记录触发的 drop/dragover/全局监听顺序
- [ ] 1.2 确认 SHA 去重未覆盖的并发路径（如局部 drop 与全局 drop 同时触发）并列出影响范围

## 2. 方案收敛
- [ ] 2.1 确定事件处理策略：阻止不必要的冒泡/默认行为或区分来源，避免双重调用 addFiles
- [ ] 2.2 设计“处理中去重”策略（如 pending 集合/队列锁），保证并发 addFiles 也不重复入队

## 3. 实施
- [ ] 3.1 更新拖拽/全局 drop 与 addFiles 去重逻辑，确保同一文件一次交互只入队一次且顺序稳定
- [ ] 3.2 完善 dragOver/loading/error 状态清理与提示，覆盖移动端拖拽场景

## 4. 验证
- [ ] 4.1 手动验证：移动端拖拽、桌面拖拽、文件选择重复上传，确认无重复入队并保持顺序
- [ ] 4.2 自动化/单元：为 addFiles 去重/并发路径补充测试用例（含重复 drop 场景）
