# Canvas-first 工作台重构

## 目标

把当前纵向堆叠的裁切工具重构为移动端 Canvas-first 工作台：调整与导出成为唯一常驻主操作，低频设置按需进入抽屉或二级页；PC 使用按需三栏布局。

## 成功标准

- 移动端上传图片后，画布和“调整 / 导出”在首屏可达。
- 只有调整模式允许编辑分割线，视觉细线拥有至少 32px 的触控热区。
- 调整采用可拖拽底部面板；预设与导入导出移至独立全屏层。
- 导出先显示结果抽屉；用户确认后下载。
- PC 与移动端均通过 CDP 实测，且测试、类型检查、构建通过。

## 阶段

- [x] 1. 调查现有移动端交互并完成设计决策
- [x] 2. 建立工作台状态与重构 App 信息架构
- [x] 3. 重做移动端底部面板、编辑触控与结果抽屉
- [x] 4. 重做 PC 的画布主导布局与预设全屏层
- [x] 5. CDP 验证移动端和 PC，执行测试与构建
- [ ] 6. 恢复原预览主流程并增量接入参数区与宫格折叠
- [ ] 7. 用 `image-sli.localhost:5188` 回归 PhotoSwipe、下载和折叠状态

## 关键决策

- 移动端固定底栏只保留“调整”和“导出”。
- 分割线仅在调整模式可拖动。
- 调整面板可在约 35% 和 75% 屏高之间拖拽。
- 高频控制是网格、线条和 padding；预设、导入导出、格式和质量属于更多设置。
- 导出先打开结果抽屉；预设库为独立全屏二级页。
- PC 是左侧轻导航、中央画布和按需右侧检查器；结果使用底部抽屉。
- 恢复原 `ResultsPanel + PhotoSwipe` 预览链路，参数区只作为网格选择后的折叠增量。
- 宫格首次生成展开，收起时仍保留切片数量、预览全部和下载全部。

## 错误记录

| 时间 | 现象 | 处理 |
|---|---|---|
| 2026-07-15 | agent-browser 的 `fill` 未触发 number input 的 Vue `input` 监听 | CDP 验证时改用真实键盘输入 |
| 2026-09-14 | `Read` 对 Markdown 传入空 `pages` 被拒绝 | 改为明确使用 `pages: "1"`，后续非 PDF 不再传空字符串 |
| 2026-09-14 | `WebFetch` 无法验证 MDN 域名安全性 | 不重试同一调用；本轮设计以仓库事实为主，外部存储约束仅在拿到可核验来源后引用 |

## 目录直写批量导出（2026-07-16，待审批）

### 目标

在 Chromium 浏览器中让“下载全部”首次选择目录、后续同目录直接批量写入；不支持或无权限时保持现有逐张下载，不引入 ZIP。

### 阶段

- [x] 1. 调查 File System Access API、传统下载和兼容性
- [x] 2. 完成产品决策、ADR 与术语建模
- [x] 3. 创建 OpenSpec 提案与实施计划
- [x] 4. 等待用户审批提案
- [x] 5. 实现目录导出 module、UI 与降级行为
- [ ] 6. 单元测试、浏览器手工回归、类型检查和构建验证（仅真实目录选择/落盘待用户手动确认）

## 剪裁结果缓存设计调查（2026-09-14，进行中）

### 目标

调查图片拖入、预览、单图下载与下载全部之间的重复剪裁路径，明确同一输入图与同一套有效参数的缓存身份、容量 30 的淘汰策略、存储层级及失效边界；完成渐进式需求拷问后再提出实施方案。

### 阶段

- [x] 1. 还原剪裁计算与各消费入口的完整调用链
- [x] 2. 盘点参数、输入身份、输出对象生命周期及现有 IndexedDB 能力
- [x] 3. 对比内存、IndexedDB 与分层缓存方案的收益、复杂度和风险
- [x] 4. 使用 `/pro-grilling` 渐进确认产品语义与工程取舍
- [x] 5. 输出共同确认的设计边界与后续实施计划

### OpenSpec 状态

- [x] 已通过 Workflow 在主工作区新增并对抗审查 `cache-slice-results-and-persist-settings`
- [x] `openspec validate cache-slice-results-and-persist-settings --strict` 通过
- [x] 等待用户审批提案后启动实现 Workflow
- [x] Workflow B：缓存 module、设置持久化、生成链路与 App 接线
- [x] 自动化验证与浏览器 QA（真实系统目录选择器仅完成源码顺序与安全降级验证，仍需人工确认原生弹窗）
- [ ] 全部验证通过后执行 `/git-up -pcP`：规划提交、提交并自动 push；若验证或提交失败则不 push

### 已确认设计决策

- 仅使用当前页面会话内的内存缓存，不使用 IndexedDB。
- 1 个缓存条目是一张输入图在一套有效参数下生成的完整切片结果集。
- 使用 LRU 双上限：最多 30 条、Blob 总量最多 256 MiB；任一超限即淘汰最久未使用条目。
- 单条超过 256 MiB 时正常用于当前预览/导出，但不进入缓存。
- 用户重置时清空全部缓存并释放相关内存，不增加独立缓存管理入口。

## 紧凑工作区与导出进度（2026-07-16，进行中）

- [x] 1. 审计现有桌面/移动页面与完成需求拷问
- [x] 2. 建立 OpenSpec 变更规格
- [x] 3. 实现分阶段 fixed 进度与完成报告
- [x] 4. 实现桌面双栏、移动单栏折叠及统一紧凑样式
- [x] 5. 完成功能回归、CDP 审计、测试、类型检查和构建（真实目录选择/落盘仍由用户确认）
